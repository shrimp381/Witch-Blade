{{!-- dnd5e sheet part: the tab wrapper around the shared Ferality body. --}}
<section class="tab {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
  {{> "modules/witch-blade/templates/witchblade-body.hbs" }}
</section>
